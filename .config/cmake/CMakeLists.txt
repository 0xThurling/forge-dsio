# This file is generated by Forge. DO NOT TOUCH


set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
      

# --- pkg-config dependencies ---
find_package(PkgConfig REQUIRED)
pkg_check_modules(LIBURING REQUIRED IMPORTED_TARGET liburing)


include(FetchContent)

# --- Dependencies ---
FetchContent_Declare(forgefp SOURCE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../fp")
FetchContent_MakeAvailable(forgefp)


# --- Build Flags ---
set(CMAKE_CXX_COMPILER_LAUNCHER ccache)
set(CMAKE_C_COMPILER_LAUNCHER ccache)
add_compile_options("$<$<CXX_COMPILER_ID:GNU,Clang,AppleClang>:-Wall>")
add_compile_options("$<$<CXX_COMPILER_ID:GNU,Clang,AppleClang>:-Wextra>")
add_compile_options("$<$<CXX_COMPILER_ID:GNU,Clang,AppleClang>:-Wpedantic>")
add_compile_options("$<$<CXX_COMPILER_ID:MSVC>:/W4>")


# --- Project Target ---
file(GLOB_RECURSE SOURCES RELATIVE ${PROJECT_SOURCE_DIR} ${PROJECT_SOURCE_DIR}/src/*.cpp)
list(FILTER SOURCES EXCLUDE REGEX "/(build|build-[^/]*|CMakeFiles)/")
if(SOURCES)
  add_library(dsio STATIC ${SOURCES})
  target_include_directories(dsio PUBLIC $<BUILD_INTERFACE:${PROJECT_SOURCE_DIR}/include> $<INSTALL_INTERFACE:include>)
  target_include_directories(dsio PRIVATE ${PROJECT_SOURCE_DIR}/src)
  install(TARGETS dsio EXPORT dsioTargets
    ARCHIVE DESTINATION lib
    LIBRARY DESTINATION lib
    RUNTIME DESTINATION bin
    INCLUDES DESTINATION include)
  install(DIRECTORY ${PROJECT_SOURCE_DIR}/include/ DESTINATION include)
  install(EXPORT dsioTargets
    FILE dsioTargets.cmake
    NAMESPACE dsio::
    DESTINATION lib/cmake/dsio)
  file(GENERATE OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/dsioConfig.cmake" CONTENT [=[
# Generated by Forge: find the packages the targets file references.
include(CMakeFindDependencyMacro)
find_dependency(forgefp)
include("${CMAKE_CURRENT_LIST_DIR}/dsioTargets.cmake")
]=])
  install(FILES "${CMAKE_CURRENT_BINARY_DIR}/dsioConfig.cmake"
    DESTINATION lib/cmake/dsio)
else()
  message(WARNING "No source files found in src/. Creating header-only INTERFACE library 'dsio'.")
  add_library(dsio INTERFACE)
  target_include_directories(dsio INTERFACE $<BUILD_INTERFACE:${PROJECT_SOURCE_DIR}/include> $<INSTALL_INTERFACE:include>)
  install(DIRECTORY ${PROJECT_SOURCE_DIR}/include/ DESTINATION include)
  install(TARGETS dsio EXPORT dsioTargets INCLUDES DESTINATION include)
  install(EXPORT dsioTargets
    FILE dsioTargets.cmake
    NAMESPACE dsio::
    DESTINATION lib/cmake/dsio)
  file(GENERATE OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/dsioConfig.cmake" CONTENT [=[
# Generated by Forge: find the packages the targets file references.
include(CMakeFindDependencyMacro)
find_dependency(forgefp)
include("${CMAKE_CURRENT_LIST_DIR}/dsioTargets.cmake")
]=])
  install(FILES "${CMAKE_CURRENT_BINARY_DIR}/dsioConfig.cmake"
    DESTINATION lib/cmake/dsio)
endif()



# --- Linking ---
if(SOURCES)
  target_link_libraries(dsio PRIVATE $<BUILD_INTERFACE:forgefp>$<INSTALL_INTERFACE:forgefp::forgefp> PkgConfig::LIBURING)
elseif(TARGET dsio)
  target_link_libraries(dsio INTERFACE $<BUILD_INTERFACE:forgefp>$<INSTALL_INTERFACE:forgefp::forgefp> PkgConfig::LIBURING)
endif()
      

# --- Testing and benchmarks ---
# Only the top-level project builds these; a consumer of this project
# does not build (or fetch a framework for) another project's tests.
if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR OR FORGE_BUILD_DEPENDENCY_TESTS)
  enable_testing()
  include(FetchContent)
  set(FETCHCONTENT_SOURCE_DIR_GOOGLETEST "/home/jacques/.cache/forge/deps/googletest-4849be21" CACHE PATH "" FORCE)
  FetchContent_Declare(googletest GIT_REPOSITORY "https://github.com/google/googletest.git" GIT_TAG "v1.14.0")
  FetchContent_MakeAvailable(googletest)
  include(GoogleTest)
  file(GLOB_RECURSE TEST_SOURCES "${PROJECT_SOURCE_DIR}/test/*.cpp")
  list(FILTER TEST_SOURCES EXCLUDE REGEX "/(build|build-[^/]*|CMakeFiles)/")

  # Build list of sources WITHOUT src/main.cpp (to avoid multiple main)
  file(GLOB_RECURSE APP_SOURCES "${PROJECT_SOURCE_DIR}/src/*.cpp")
  list(FILTER APP_SOURCES EXCLUDE REGEX "/(build|build-[^/]*|CMakeFiles)/")
  list(FILTER APP_SOURCES EXCLUDE REGEX ".*main\\.cpp$")

  add_executable(${PROJECT_NAME}_tests ${TEST_SOURCES} ${APP_SOURCES})
  target_include_directories(${PROJECT_NAME}_tests PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/src
    ${CMAKE_CURRENT_SOURCE_DIR}/include
  )
  target_link_libraries(${PROJECT_NAME}_tests PUBLIC GTest::gtest_main forgefp PkgConfig::LIBURING)
  gtest_discover_tests(${PROJECT_NAME}_tests)

  # --- Benchmarks ---
  FetchContent_Declare(googlebenchmark
    GIT_REPOSITORY "https://github.com/google/benchmark.git"
    GIT_TAG "v1.8.3")
  set(BENCHMARK_ENABLE_TESTING OFF CACHE BOOL "" FORCE)
  set(BENCHMARK_ENABLE_WERROR OFF CACHE BOOL "" FORCE)
  FetchContent_MakeAvailable(googlebenchmark)

  file(GLOB_RECURSE BENCH_SOURCES "${PROJECT_SOURCE_DIR}/bench/*.cpp")
  if(BENCH_SOURCES)
    add_executable(${PROJECT_NAME}_bench ${BENCH_SOURCES})
    target_link_libraries(${PROJECT_NAME}_bench PRIVATE benchmark::benchmark ${PROJECT_NAME} forgefp PkgConfig::LIBURING)
    target_include_directories(${PROJECT_NAME}_bench PRIVATE
      ${PROJECT_SOURCE_DIR}/src
      ${PROJECT_SOURCE_DIR}/include)
  endif()

endif()


